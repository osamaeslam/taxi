# 📋 UserReport - بوت تكسي واتساب (whatsapp-taxi-dispatch)

## 🏗️ نظرة عامة
نظام متكامل لحجز وإدارة سيارات الأجرة عبر تطبيق واتساب لمحافظة حماة بسوريا. يعتمد على معمارية هجينة تفصل بين منطق العمل ولوحة التحكم السحابية (Cloudflare Worker + D1 Database) وبوابة الاتصال المحلية بواتساب (Node.js Baileys Gateway) لضمان الاستقرار وسرعة الاستجابة ومنع تكرار اقتران الجلسة.

## 📝 سجل التغييرات
| التاريخ | التغيير | الملفات المتأثرة |
|---------|---------|------------------|
| 2026-09-06 | سحب المستودع وتثبيت كافة تبعيات الـ Worker وبوابة واتساب | `package.json`, `gateway/package.json` |
| 2026-09-06 | إنشاء هجرة 0006: سيدر شامل لمحافظة حماة (98 منطقة موزعة على 3 أحزمة جغرافية) وتعبئة أكثر من 65 خط رحلات معتمد بأسعار حقيقية | `migrations/0006_hama_comprehensive_seed.sql` |
| 2026-09-06 | إصلاح مشكلة فقدان جلسة واتساب عند إعادة التشغيل وتأمين بقاء ملف `creds.json` دون مسح أو حجر تلقائي | `gateway/gateway.mjs` |
| 2026-09-06 | إضافة سكريبت تشغيل فوري لويندوز `run-gateway.bat` وإضافة أمر `npm run gateway` في `package.json` | `run-gateway.bat`, `package.json` |
| 2026-09-06 | توثيق معمارية المشروع وسجل الذاكرة الدائمة وفق بروتوكول Antigravity | `Agent.md`, `UserReport.md` |
| 2026-09-07 | مزامنة قاعدة البيانات الحية على Cloudflare D1 بكامل مناطق حماة (98 منطقة) وتعبئة 84 خط أسعار معتمد، واختبار الروابط الحية | `scripts/seed-remote.mjs`, Cloudflare D1 |
| 2026-09-07 | إضافة زر الشمس والقمر (ليلي/نهاري) للوحة الإدارة مع التكيف التلقائي مع ثيم نظام التشغيل وحفظ التفضيل محلياً | `src/admin.ts` |
| 2026-09-07 | ترقية مكتبة `@whiskeysockets/baileys` من 6.7.18 إلى 6.7.22 وتفعيل مراقبة الطبقة التحتية للسوكت `CB:message` وفك قيود الفلترة في `messages.upsert` لدعم رسائل `notify` و `append` | `gateway/package.json`, `gateway/gateway.mjs` |
| 2026-09-07 | إضافة كاش إعادة المحاولة `msgRetryCounterCache` لفك تشفير Signal، وإضافة إعلان التواجد `sendPresenceUpdate('available')` عند فتح الاتصال، وربط أحداث `messaging-history.set` و `messages.update` و `messages.reaction` | `gateway/gateway.mjs` |

## 🐛 المشاكل والحلول
| المشكلة | الحالة | الحل |
|---------|--------|------|
| مسح الجلسة وطلب رمز الاستجابة السريعة (QR) عند كل تعديل أو إعادة تشغيل للبوابة | محلولة | تعديل دالة التحقق من الجلسة `isReallyRegistered` لمنع الخطأ في قراءة معرف الحساب، وإلغاء أمر الحجر الآلي `quarantineSession` عند بدء التشغيل طالما الجلسة مسجلة. |
| اختصار مناطق حماة سابقاً إلى 21 حياً ومسح كافة أسعار الرحلات الثابتة | محلولة | تدقيق مناطق محافظة حماة بالكامل استناداً للخرائط والمصادر الجغرافية، وتقسيمها إلى 51 حياً داخلياً، 20 ضاحية وقرية ملاصقة، و 27 مركز منطقة وناحية وبلدة ريفية، مع تعبئة أكثر من 65 خط رحلات معتمد. |
| تشغيل vitest محلياً قبل تثبيت حزم الجذر | محلولة | تنفيذ `npm install` في جذر المشروع لتثبيت أدوات التطوير وفحص التجميع. |
| عدم وصول الرسائل الواردة إلى لوحة البوت رغم ظهورها على الهاتف (Deaf Session / Inbound Drop) | قيد التشخيص والاختبار | ترقية Baileys إلى 6.7.22 لمعالجة خلل تزامن الرسائل، إضافة مراقبة منبع السوكت `CB:message` وتضمين رسائل `append` المشابهة لـ `notify` لمنع الإسقاط الصامت للرسائل غير المصنفة. |

## 💻 أخطاء التيرمينال
| الأمر | الخطأ | الحل |
|-------|-------|------|
| `npm test` | `'vitest' is not recognized as an internal or external command` | تم تشغيل `npm install` في الجذر لتثبيت كافة حزم التطوير ومنها vitest بنجاح. |
| `run-gateway.bat` | `RKER_URL""=="" (' is not recognized ...` | مشكلة شهيرة في مفسر `cmd.exe` في ويندوز عند قراءة حروف عربية متعددة البايتات (UTF-8 buffer drift)؛ تم إنشاء مشغل Node مستقل `gateway/start.mjs` وجعل `run-gateway.bat` سكريبت ASCII خفيف من 3 أسطر فقط. |
