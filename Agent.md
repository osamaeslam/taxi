# 🧠 Agent.md - whatsapp-taxi-dispatch

## 📊 نظرة عامة
- **اسم المشروع:** whatsapp-taxi-dispatch (بوت حجز تاكسي حماة عبر واتساب)
- **نوع المشروع:** Serverless Edge API + Multi-page Web Admin + Realtime WhatsApp Baileys Gateway
- **اللغة:** TypeScript / JavaScript (Node.js 22+ / ES Modules)
- **الإطار:** Cloudflare Workers + Cloudflare D1 Database (SQLite) + Baileys (WhatsApp Web Multi-Device)
- **نقاط الدخول:**
  - `src/worker.ts`: خادم الـ Worker السحابي ومعالجة Webhooks ولوحة الإدارة
  - `gateway/gateway.mjs`: بوابة الاتصال المحلية بواتساب (Baileys)

## 🌲 المخطط الشجري
```text
whatsapp-taxi-dispatch/
├── gateway/                    # بوابة واتساب المحلية المستقلة (Baileys)
│   ├── gateway.mjs             # الكود الأساسي للبوابة وسحب Outbox والاقتران
│   ├── gen-qr-once.mjs         # توليد QR لمرة واحدة للاختبار
│   ├── package.json            # تبعيات البوابة (@whiskeysockets/baileys, pino, qrcode)
│   └── session/                # مجلد بيانات الاعتماد ومفاتيح الجلسة (محمي)
├── migrations/                 # ملفات هجرة قاعدة بيانات Cloudflare D1
│   ├── 0001_init.sql           # الجداول الأساسية (zones, fixed_fares, drivers, rides, ...)
│   ├── 0002_processed.sql      # جدول منع تكرار الرسائل
│   ├── 0003_hama_seed.sql      # السيدر التجريبي الأولي
│   ├── 0004_messages.sql       # سجل الرسائل والإعدادات
│   ├── 0005_hama_city_reseed.sql # تنظيف الأحياء السابقة
│   └── 0006_hama_comprehensive_seed.sql # سيدر حماة الشامل الموثق (98 منطقة + 67 خط ثابت)
├── src/                        # كود المصدر للـ Cloudflare Worker
│   ├── admin.ts                # واجهة لوحة التحكم ومسارات صفحات الإدارة
│   ├── ai.ts                   # تكامل الذكاء الاصطناعي (Z.AI / GLM)
│   ├── engine.ts               # محرك البوت ومعالجة الرسائل وتوزيع الطلبات
│   ├── nlu.ts                  # المحلل اللغوي العامي واستخراج النوايا والمناطق
│   ├── pricing.ts              # خوارزمية التسعير بالأحزمة والأسعار الثابتة
│   ├── repo.ts                 # طبقة المستودعات واستعلامات D1
│   ├── ride-state.ts           # آلة حالات الرحلة (State Machine)
│   ├── types.ts                # تعريفات الأنواع في TypeScript
│   ├── whatsapp-tab.ts         # تبويب إدارة الواتساب من اللوحة
│   └── worker.ts               # نقطة الدخول والموجّه (Router)
├── tests/                      # اختبارات Vitest للـ NLU والتسعير والحالات
│   ├── nlu.test.ts             # اختبارات اللهجة العامية ومطابقة المناطق
│   └── pricing-state.test.ts   # اختبارات التسعير وآلة الحالات
├── run-gateway.bat             # مشغل البوابة السريع على بيئة ويندوز
├── package.json                # إعدادات المشروع واختبارات Vitest
├── tsconfig.json               # إعدادات TypeScript
└── wrangler.toml               # إعدادات Cloudflare Worker و D1
```

## 🛠️ أوامر التشغيل
| الأمر | الوظيفة |
|-------|---------|
| `npm test` | تشغيل اختبارات NLU وآلة الحالات والتسعير |
| `npx tsc --noEmit` | فحص سلامة الأنواع والتجميع (Type Check) |
| `npm run gateway` | تشغيل بوابة واتساب من خلال Node.js |
| `run-gateway.bat` | تشغيل فوري للبوابة على ويندوز مع إدخال ADMIN_KEY |
| `wrangler dev` | تشغيل Worker محلياً للاختبار |

## 📦 التبعيات الرئيسية
| المكتبة | الإصدار | الوظيفة |
|---------|---------|---------|
| `@whiskeysockets/baileys` | `6.7.22` | اتصال بواتساب ويب عبر WebSocket وإدارة المصادقة (إصدار مصحح لثغرة مزامنة الرسائل) |
| `@cloudflare/workers-types` | `^4.20250830.0` | أنواع بيئة Cloudflare Workers و D1 |
| `vitest` | `^2.1.0` | منصة اختبارات الوحدة المتزامنة والسريعة |
| `qrcode` | `^1.5.4` | توليد صور QR لربط واتساب من لوحة الإدارة |
| `pino` | `^9.4.0` | تسجيل الأحداث (Logging) عالي الأداء للبوابة |

## ✅ أفضل الممارسات المكتشفة
- **فصل الاتصال عن المعالجة (Decoupled Gateway):** الـ WebSocket يبقى متصلاً باستمرار على اللابتوب أو السيرفر، بينما يعالج الـ Worker الطلبات كـ Webhooks عديمة الحالة (Stateless).
- **الحفاظ على الجلسة (Session Persistence):** مفاتيح الجلسة مخزنة في `gateway/session/creds.json`؛ لا يتم مسحها إطلاقاً عند إعادة التشغيل أو تعديل الكود لضمان عدم فقدان الربط.
- **تدرج التسعير في حماة:** التعرفة اليدوية المعتمدة (`fixed_fares`) لها الأولوية القصوى، وفي حال عدم وجودها يتم الاعتماد على صيغة الأحزمة (1: داخل المدينة 10-15 ألف، 2: الضواحي 30-40 ألف، 3: الريف 60-170 ألف).
- **مقاومة الحظر (Anti-Ban Jitter):** إرسال الرسائل عبر Outbox بفاصل زمني عشوائي (400-800ms) لمنع رصد النشاط كـ Spam من قبل خوارزميات واتساب.

## ⚠️ المشاكل المعروفة والحلول
| المشكلة | السبب | الحل | التاريخ |
|---------|-------|------|---------|
| مسح الجلسة وطلب QR مع كل تعديل | دالة `isReallyRegistered` كانت تفترض وجود `:` في `me.id`، وفحص الإقلاع كان يحجر الجلسة إذا اعتبرها ناقصة | توسيع الدالة وإلغاء المسح التلقائي عند الإقلاع طالما الجلسة مسجلة | 2026-09-06 |
| فقدان أسعار الرحلات في سيدر 0005 | قيام الوكيل السابق بحذف كافة الـ fixed_fares واقتصار المناطق على 21 حياً | إنشاء هجرة 0006 تضم 98 منطقة شاملة و 67 خطاً معتمداً للأسعار | 2026-09-06 |
| تضارب البوابات وحظر الرقم | تشغيل بوابتين لنفس الرقم في وقت واحد (سيرفر + لابتوب) | إيقاف بوابة السيرفر تماماً قبل أي تشغيل من اللابتوب المنزلي | 2026-09-06 |
| ظاهرة الجلسة الصامتة (Deaf Session) | إصدار Baileys القديم 6.7.18 به خلل تزامن، وفلترة الرسائل تُسقط أحداث append الصامتة | ترقية Baileys إلى 6.7.22، فك تقييد notify ليشمل append، وإضافة مراقبة CB:message | 2026-09-07 |

## 🚫 أنماط يجب تجنبها
- **ممنوع تشغيل بوابتين متزامنتين بنفس الرقم:** يؤدي لطرد الجلسة فوراً وزيادة مدة الحظر المؤقت في واتساب.
- **ممنوع استخدام wrangler التفاعلي:** تجنب الأوامر التي تفتح جلسات تفاعلية أو WSL كما هو منصوص عليه في البروتوكول.
- **ممنوع مسح مجلد `session/` إلا بطلب إداري صريح:** مسح الملفات يعني إلغاء الاقتران واضطرار المستخدم لمسح QR جديد.

## 🔗 ملاحظات البيئة
- **متغيرات الـ Worker:**
  - `SERVICE_NAME`: اسم الخدمة
  - `AI_BASE_URL`: رابط مزود الذكاء الاصطناعي (Z.AI)
  - `AI_MODEL`: نموذج `glm-5.3-flash`
  - `DB`: قاعدة بيانات D1 السحابية (`taxi-dispatch`)
- **متغيرات البوابة:**
  - `WORKER_URL`: رابط الـ Worker السحابي
  - `ADMIN_KEY`: المفتاح السري للإدارة والتوثيق
  - `GATEWAY_PORT`: منفذ HTTP الداخلي (الافتراضي: 3010)

## 📚 مراجع مفيدة
- [Cloudflare D1 Documentation](https://developers.cloudflare.com/d1/)
- [Baileys WhatsApp Library](https://github.com/WhiskeySockets/Baileys)
- [Vitest Testing Guide](https://vitest.dev/)

## 🧹 صيانة الذاكرة (Memory Hygiene)
### آخر تنظيف: 2026-09-06
- تم التحقق من سلامة كافة الاختبارات (31/31 ناجحة).
- تم التحقق من تجميع TypeScript بنجاح.
